*.dll
*.dylib
*.so
*.h
# built shared libraries (root dist/ only)
/dist/
.gomodcache/
.gocache/
.go-appdata/
.go-localappdata/
cpa-key-policy-state.json
cpa-key-quota-state.json
**/cpa-key-quota-state.json
coverage.out

# CPA reference checkout used for research, not part of this project
_cpa_ref/
# Official panel reference checkout used for research, not part of this project
_panel_ref/

# web frontend build output (rebuilt by `make web-build`)
web/node_modules/
web/dist/
# tsc -b composite project artifacts
web/*.tsbuildinfo
web/vite.config.d.ts
web/vite.config.js
web/vitest.config.d.ts
web/vitest.config.js

# NOTE: internal/plugin/web/dist/index.html IS tracked. It is the go:embed
# source for the hosted UI. `make web-build` (and CI) overwrite it with a fresh
# `VITE_HOSTED=1 npm run build`; the committed copy is that real build, so a
# plain `go build` embeds the shipped UI.

node_modules/

stitch_*.png

# Live gomami tunnel config (UUID + public hostname). Never commit.
e2e/plus/gomami-cloudflared-config.yml
e2e/plus/cloudflared/
.npm-cache/
